namespace Game.Scripts
{
    public class GameManager
    {
        private const string Root = "Game/";
        private const string Create = Root + "Create/";
        private const string Resource = "Resource/";
        private const string ResourceSource = "Resource Source/";
        internal const string CreateResourceProfile = Create + Resource;
        internal const string CreateResourceSourceProfile = Create + ResourceSource;
    }
}